[package]
name = "password_api"
version = "0.1.0"
edition = "2021"

[dependencies]
actix-web = "4"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
password_engine = { path = "../engine" }
tokio = { version = "1", features = ["full"] }
